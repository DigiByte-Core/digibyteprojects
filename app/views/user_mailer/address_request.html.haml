%h4 Hello,

%p
  #{@collaborator.nickname} wants to reward you #{@tip.amount ? btc_human(@tip.amount) : ""} on his project #{@tip.project.name}.
  = link_to "More details on the distribution", project_distribution_url(@tip.project, @tip.distribution)

%p
  To get your reward you must provide a DigiByte address.

- if @user.confirmed?
  %p= link_to "Set your DigiByte address", edit_registration_url(@user)
- else
  %p= link_to 'Set your password and DigiByte address', set_password_and_address_user_url(@user, token: @user.confirmation_token)

%p Thank you.

%p= link_to "Digis4Commits.com", "http://Digis4Commits.com/"

%p
  %small
    = link_to "Don't notify me anymore.", login_users_url(token: @user.login_token, unsubscribe: true)

